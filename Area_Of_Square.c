#include<stdio.h>
    int main() {
                  int side, area;
                  printf("Give the Length of Side : ");
                  scanf("%d", &side);
                  area = side * side;
                  printf("\n\n The Area of Square is : %d", area);
    return (0);
    }